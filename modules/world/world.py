import pygame

# Constants for the screen dimensions and colors
WIDTH, HEIGHT = 800, 800  # Dimensions of the game window
WHITE, BLACK, RED, BLUE = (255, 255, 255), (0, 0, 0), (255, 0, 0), (0, 0, 255)  # RGB color definitions

# Clock for controlling the game's frame rate
clock = pygame.time.Clock()

# Frames per second setting for the game loop
FPS = 60
